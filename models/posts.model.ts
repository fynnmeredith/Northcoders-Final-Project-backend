import { db } from "../db/connection";

export const insertPost = () => {
  return db.query(``).then((res) => {
    return res;
  });
};

export const removePost = () => {
  return db.query(``).then((res) => {
    return res;
  });
};

export const selectPostByUser = () => {
  return db.query(``).then((res) => {
    return res;
  });
};

export const selectPostByUserFriends = () => {
  return db.query(``).then((res) => {
    return res;
  });
};
